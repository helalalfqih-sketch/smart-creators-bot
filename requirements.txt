# cache-bust: 2026-05-21-v3 — force fresh Docker build with .env included
python-telegram-bot>=22.0,<23.0
aiohttp>=3.10,<4.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
yt-dlp[default,curl-cffi]>=2026.3.17
redis>=5.0,<6.0        # optional – graceful fallback to memory cache if absent
rq>=2.0,<3.0           # Redis Queue for background jobs
requests>=2.32,<3.0    # sync HTTP client for bot → API gateway
faster-whisper>=1.0.0  # speech transcription for pro captions feature
python-dotenv>=1.0.0   # load .env file automatically
